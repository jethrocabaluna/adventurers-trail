<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Adventurer's Trail</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/css/app.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>
  <script src="https://use.fontawesome.com/7412943e2f.js"></script>
</head>
<body>

  <nav class="navbar sticky-top navbar-expand-lg">
    <div class="container">
      <a class="navbar-brand" href="/adventures">Adventurer's Trail</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#Authentication" aria-controls="Authentication"
        aria-expanded="false" aria-label="Toggle navigation">
        <span><i class="fa fa-bars"></i></span>
      </button>
    
      <div class="collapse navbar-collapse" id="Authentication">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/adventurers">Adventurers</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/posts">Posts</a>
          </li>
          <% if(!currentUser){ %>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/signup">Sign Up</a>
            </li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/posts/new">Create Post</a></li>
            <li class="nav-item"><a class="nav-link" href="/adventurers/<%= currentUser.username %>"><%= currentUser.username %></a></li>
            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
  <% if(error && error.length > 0){ %>
    <!-- check if there is error/success variable and it's length is greater than 0 -->
    <div class="flash-message container">
      <div class="alert alert-danger">
        <p>
          <%= error %>
        </p>
      </div>
    </div>
    <% } else if(success && success.length > 0){ %>
      <div class="flash-message container">
        <div class="alert alert-success">
          <p>
            <%= success %>
          </p>
        </div>
      </div>
      <% } %>